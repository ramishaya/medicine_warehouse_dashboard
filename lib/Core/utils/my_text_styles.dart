import 'package:flutter/material.dart';

class MyTextStyles {
  static const Shadow textShadow = Shadow(
    blurRadius: 4.0,
    color: Colors.black45,
    offset: Offset(2.0, 2.0),
  );

  static const FontWeight thinTextWeight = FontWeight.w300;
  static const double bigTextSize = 32;
  static const Color  mytextColor = Color(0xFF62d0bd);
}